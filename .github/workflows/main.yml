name: First Workflow


#on: [push, issues, pull_request]
# manual workflow only works with the default branch
on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo 'build'

  test:
    runs-on: windows-latest
    steps:
      - name: Print test - Chrome
        run: echo 'build'
      - name: Print test - edge
        run: echo 'test edge'

  
  
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print deploy
        run: echo 'deploy'
